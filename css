/* styles.css */

body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #000000;
   
}
.image-container {
    position: relative;
    width: 100%; /* Adjust as needed */
    height: 100vh; /* Adjust height as needed (viewport height in this case) */
    overflow: hidden; /* Ensures no scrollbars are shown */
}

.background-image {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    object-fit: cover; /* Ensures the image covers the container */
    transform: translate(-50%, -50%); /* Centers the image */
}
.text-and-button {
    position: relative; /* Allows for absolute positioning of child elements */
    text-align: center; /* Center text and button */
    color: white; /* Text color for contrast */
    z-index: 1; /* Ensure text and button are on top of the background image */
}

.centered-text {
    position: relative;
    color: white; /* Text color for contrast */
    font-size: 6rem; /* Adjust font size as needed */
    text-align: center;
    z-index: 1; /* Ensure text is on top of the background image */
    margin-top: 280px;
    font-family: Curlz MT;
}


.centered-button {
    font-family: 'Times New Roman', Times, serif;
    font-size: 1.25rem; /* Adjust font size as needed */
    padding: 0.5rem 1rem; /* Add padding to the button */
    color: white; /* Button text color */
    background-color: #6b4307; /* Button background color */
    border: none; /* Remove default border */
    border-radius: 1rem; /* Rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    transition: background-color 0.3s; /* Smooth background color transition */
}

.centered-button:hover {
    background-color: #022347; /* Darker shade on hover */
}

/* General styling for the section */
.hidden add-recipe {
    display: none; /* Hidden by default */
    padding: 20px;

    
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    max-width: 600px;
    margin: 20px auto;

}
.hidden {
    display: none;
   
  
}

/* Styling the Add Recipe section */
.hidden add-recipe {
    max-width: 600px;
    margin: 50px auto;
    background-color: #ffffff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    margin-top: 50px;
}

/* Section heading */
.hidden add-recipe h2 {
    text-align: center;
    color: #555;
    margin-bottom: 20px;
}

/* Form label styling */
.hidden add-recipe label {
    display: block;
    margin-bottom: 8px;
 
    font-weight: bold;
}

/* Form input and textarea styling */
.hidden add-recipe input[type="text"],
.hidden add-recipe textarea,
.hidden add-recipe select {
    width: 600px;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
    box-sizing: border-box;
}

/* Button styling */
.hidden add-recipe button {
    width: 500px;
    padding: 10px;
    background-color: #28a745;
    color: #ffffff;
    border: none;
    border-radius: 4px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

/* Button hover effect */
.hidden add-recipe button:hover {
    background-color: #218838;
}

/* Additional button styling for back-to-home button */
#add-recipe #back-to-front-page {
    background-color: #6c757d;
    margin-top: 10px;
}

#add-recipe #back-to-front-page:hover {
    background-color: #5a6268;
}

/* Responsive Design */
@media (max-width: 600px) {
    #add-recipe {
        padding: 15px;
        margin: 20px;
    }

    #add-recipe h2 {
        font-size: 24px;
    }
}

#front-page #goto-add-recipe{
    border-width: 5px;
    border-radius: 6px;
    font-size: 14px;
  
    margin-left: 100px;
}



#front-page #goto-add-recipe:hover{
    background-color: #9fb3b3;
}

#front-page #goto-recipe-list{
    border-width: 5px;
    border-radius: 6px;
    font-size: 14px;
    margin-left: 30px;

}

#front-page #goto-recipe-list:hover{
    background-color: #9fb3b3;
}
/* Styles for the Recipe Details section */
#recipe-details {
    background: white;
    border-radius: 5px;
    padding: 1rem;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    margin-top: 2rem;
   
}

#recipe-details h2 {
    color: #030303;
    margin-bottom: 1rem;
    font-family: 'Times New Roman', Times, serif;
    padding-left: 700px;
}

#details {
    font-size: 1rem;
    line-height: 2.5;
}

#details h3 {
    color: #333;
    margin-bottom: 0.5rem;
}

#details p {
    margin: 0.5rem 0;
}

#details p strong {
    color: #555;
}

/* Back Button */
#recipe-details #back-to-recipe-list {
    width: 100%;
    padding: 10px;
    background-color: #007bff;
    color: #fff;
    border: none;
    border-radius: 4px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 20px;
}

#recipe-details #back-to-recipe-list:hover {
    background-color: #0056b3;
}


header {
   
    color: white;
    text-align: center;
    padding: 1rem 0;
}

header p {
    margin: 0.5rem 0 0;
}

main {
    padding: 2rem;
}
#front-page{
    background-color:rgb(6, 0, 0); ;
}

section {
    background: rgb(254, 253, 253); ;
    border-radius: 5px;
    padding: 1rem;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    margin-bottom: 2rem;
}
/* Section styling */
#gallery {
    padding: 2rem 0; /* Add vertical padding */
    background-color: rgb(6, 0, 0);
}

/* Gallery item styling */
.gallery-item {
    text-align: center; /* Center text below image */
    margin-bottom: 1.5rem; /* Space below each item */
}

.gallery-item img {
    width: 600px;
    height: 300px;
    border-radius: 0.5rem; /* Rounded corners for the images */
    transition: transform 0.3s ease; 
}
.gallery-item img:hover {
    transform: scale(1.1); /* Scale the image to 110% */
}

.gallery-item h2 {
    margin-top:20px;
    font-size: 1.25rem;
    font-weight: 600;
    color: #fefefe; /* Dark text color for contrast */
    font-family: 'Times New Roman', Times, serif;
   padding-right: 500px;
}

.hidden {
    display: none;
}

.item {
    text-align: center; /* Center-align the text */
  
}

.item h1 {
    color: #f1f5f5; /* Set the text color */
    font-family:'Times New Roman', Times, serif;
    font-size: 28px; /* Set the font size */
    font-weight: bold; /* Make the font bold */
  
    max-width: 800px; /* Optional: Set a maximum width for the text */
    margin: 0 auto; /* Center the text block horizontally */
    padding: 20px; /* Add some padding around the text */
  
  
}
h3{
    font-family: 'Times New Roman', Times, serif;
}

h2 {
    color: #060303;
    font-family: 'Times New Roman', Times, serif;
    padding-left: 500px;
}

form {
    display: flex;
    flex-direction: column;
    max-width: 500px;
   padding-left: 500px;
  

 
label {
    margin: 0.5rem 0 0.2rem;
}

input, textarea, select {
    margin-bottom: 1rem;
    padding: 0.5rem;
    border: 1px solid #252323;
    border-radius: 5px;
}





#recipe-list ul {
    list-style: none;
    padding: 500px;
}

#recipe-list li {
    padding: 0.5rem;
    border-bottom: 1px solid #383131;
    cursor: pointer;
}

#recipe-list li:hover {
    background-color: #f9f9f9;
}

#intro {
    text-align: center;
}

#intro-image {
    max-width: 100%;
    height: auto;
    border-radius: 5px;
    margin-top: 1rem;
}
